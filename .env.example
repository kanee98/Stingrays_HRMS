# Copy to .env and adjust. .env is git-ignored so server config is not overwritten on git push.
#
# Local: no .env needed (defaults to localhost), or set:
# PUBLIC_HOST=localhost
#
# Server: set your server hostname or IP so frontends point to the right URLs:
PUBLIC_HOST=your-server.com
