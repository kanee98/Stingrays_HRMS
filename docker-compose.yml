services:
  hrms-ui:
    build: ./hrms-ui
    ports:
      - "3000:3000"

  employee-ui:
    build: ./employee-onboarding/frontend
    ports:
      - "3001:3000"

  employee-api:
    build: ./employee-onboarding/backend
    ports:
      - "4000:4000"